
# Russian Lighthouse

Сайт музыкальной группы с блогом и системой благотворительных сборов.

---

## 📦 Технологический стек

### Backend

* Laravel 12
* PHP 8.3
* PostgreSQL 16
* Redis

### Frontend

* Vue 3 + TypeScript
* Vite (сборка и dev-сервер)
* Tailwind CSS
* Inertia.js (Vue-драйвер), маршруты через Laravel
* Алиас `@` → `resources/js` (импорты `@/Components/...`, `@/Layouts/...`)

### Admin

* Filament

### Infrastructure

* Docker (Docker Compose v2)
* Nginx (Alpine)
* Образы: PHP 8.3-fpm, PostgreSQL 16, Redis 7
* Entrypoint в контейнере app: при старте выставляются права на `storage` и `bootstrap/cache` для записи PHP-FPM (www-data).

---

## 🧰 Требования к окружению

* Docker >= 24
* Docker Compose v2 (встроен в Docker Desktop)
* Git

Локально PHP не требуется — используется контейнер.

---

## 🐘 Версия PHP

Проект работает на:

PHP 8.3

Используются возможности языка:

* strict types
* typed properties
* readonly properties
* enums
* constructor property promotion

Во всех PHP-файлах обязательно:

```php
declare(strict_types=1);
```

---

## 📐 Стандарты кодирования

Проект следует стандартам:

* PSR-1
* PSR-12
* PSR-4 (autoloading)
* PSR-3 (logging)
* PSR-7 / PSR-15 при необходимости middleware

Форматирование кода:

* Laravel Pint


## 🧱 Архитектурные правила

1. Контроллеры — только orchestration.
2. Бизнес-логика — в Service слое.
3. Валидация — через FormRequest.
4. DTO используются для передачи данных между слоями.
5. Репозитории применяются при усложнении доступа к данным.
6. Никакой бизнес-логики в контроллерах.
7. Никакой логики в Blade или Vue компонентах.
8. Feature-based / Domain структура.

Структура проекта:

```
app/Domain/
    Blog/
    Music/
    Donations/
```

---

## 🗄 База данных

* PostgreSQL 16
* Миграции через Laravel


---

## 🔐 Авторизация и роли

Используется Laravel Breeze (Vue + TypeScript). Интерфейс входа, регистрации и профиля — на русском языке.

**Роли и права** (таблицы `roles`, `permissions`, `permission_role`):

| Роль | Права (slug в коде) |
|------|----------------------|
| **Пользователь** (user) | view_pages, read_news, view_fundraisers, make_donations, listen_media |
| **Администратор** (admin) | Все права пользователя + manage_content, manage_fundraisers, manage_seo, manage_media, view_donation_stats |

После первой миграции заполнить права:  
`docker compose exec app php artisan db:seed --class=PermissionSeeder`

При регистрации назначается роль «Пользователь». Проверка в коде: `$user->hasPermission(Permission::MANAGE_CONTENT)`. Доступ в админку только у роли admin. Чтобы выдать роль администратора:

```bash
# Первому пользователю в БД:
docker compose exec app php artisan db:seed --class=AssignFirstAdminSeeder

# Конкретному по email:
docker compose exec app php artisan db:seed --class=AssignFirstAdminSeeder
# и в .env задать ADMIN_EMAIL=your@email.com перед запуском сидера
```

---

## 🌐 Язык интерфейса

* Локаль приложения по умолчанию: **русский** (`config/app.php`: `locale` → `ru`).
* Все страницы Breeze (вход, регистрация, восстановление пароля, профиль) переведены на русский.
* Админка Filament использует локаль приложения (русский для дат/форматов).

---

## 🏠 Главная страница (структура)

Главная (`/`, `resources/js/Pages/Welcome.vue`) собирается из блоков в `resources/js/Components/Home/`:

| Компонент | Назначение |
|-----------|------------|
| **HeroSlider** | Фоновый слайдер (автосмена, пропс `slides`). |
| **HeroBlock** | Hero с названием группы, описанием, CTA. |
| **NewsSlider** | Горизонтальный скролл новостей/анонсов (`items`). |
| **ActiveFundraisers** | Сетка активных сборов (`items`: title, progress, href). |
| **MediaBlock** | Видео и музыка (`videos`, `tracks`). |
| **MissionBlock** | Краткий текст миссии. |

Данные пока заглушечные; при появлении API/контроллера главной — передавать пропсы через Inertia в `Welcome.vue`.

---

## 🔄 Очереди

* Redis

Запуск worker:

```bash
docker compose exec app php artisan queue:work
```

В production рекомендуется использовать Horizon + Supervisor.

---

## 🚀 Запуск проекта

### Стабильная сборка фронта

Версии фронт-зависимостей подобраны так, чтобы сборка проходила без постоянных правок. **Не обновлять** `vite`, `laravel-vite-plugin`, `@vitejs/plugin-vue` без необходимости: возможны конфликты peer dependencies. Текущая проверенная связка: Vite 6, laravel-vite-plugin ~1.3. Установка: `npm install` (в контейнере — без флагов; на хосте при ошибках peer deps — `npm install --legacy-peer-deps`).

### Первый запуск (клонировали репозиторий)

1. Скопировать `.env.example` в `.env`.
2. Поднять контейнеры: `docker compose up -d`
3. Установить зависимости и собрать проект:

   ```bash
   docker compose exec app composer install --no-interaction
   docker compose exec app php artisan key:generate
   docker compose exec app php artisan storage:link
   docker compose exec app php artisan migrate --force
   docker compose exec app npm install
   docker compose exec app npm run build
   ```

4. Открыть в браузере: **http://localhost:8000**

### Обычный запуск (проект уже настроен)

1. Поднять контейнеры: `docker compose up -d`
2. Если нужен **hot-reload** фронта (Vue/TS/CSS без пересборки) — после каждого перезапуска контейнера app нужно снова запустить Vite:

   ```bash
   docker compose exec -d app npm run dev
   ```

3. Если hot-reload не нужен — достаточно одной сборки. Сайт работает по **http://localhost:8000**, скрипты берутся из `public/build`. После правок во фронте: `docker compose exec app npm run build`

### Установка фронта на хосте (Windows)

При установке зависимостей на хосте возможны конфликты peer dependencies (Vite / plugin-vue / laravel-vite-plugin). Тогда установить с флагом:

```bash
npm install --legacy-peer-deps
```

Запуск dev-сервера локально: `npm run dev` (не `vite` — скрипт из `package.json`).

### Адрес и роли

* **Сайт:** http://localhost:8000 — главная, вход, регистрация, панель, профиль.
* **Админка Filament:** http://localhost:8000/admin — вход под любым пользователем Breeze.
* Порт **5173** используется только Vite dev server (при `npm run dev`).
